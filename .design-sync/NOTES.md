- This repo is CSS-only (no package.json, no dist, no Storybook), so the converter does not apply. `ds-bundle/` is generated by `.design-sync/make-bundle.py` from `docs/v1/shrippen.css` (the last step of `./build.sh`). No `_ds_bundle.js` and no `_ds_sync.json` are produced; every sync re-uploads everything.
